type SendMessageResponse {
  ok: Boolean!
  error: String
}

type Mutation {
  SendMessage(
    nickname: String!
    thumbnail: String!
    contents: String!
    innerChannelId: Int!
  ): SendMessageResponse!
}
